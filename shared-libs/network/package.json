{
  "name": "@sample/network",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "echo hello network",
    "clean": "rimraf --no-glob ./coverage ./tsconfig.tsbuildinfo",
    "lint": "eslint . --ext .ts,.tsx,.js,.jsx --cache --cache-location ../../.cache/eslint/apollo.eslintcache",
    "typecheck": "tsc --project ./tsconfig.json --noEmit",
    "test-unit": "jest --config jest.config.js",
    "fix-all-files": "eslint . --ext .ts,.tsx,.js,.jsx --fix"
  },
  "dependencies": {
    "@apollo/client": "^3.7.0",
    "apollo-upload-client": "^17.0.0",
    "apollo3-cache-persist": "^0.14.1",
    "graphql": "^16.6.0",
    "@sample/utilities": "1.0.0"
  },
  "devDependencies": {
    "@sample/eslint-config-bases": "1.0.0"
  }
}
